#include <stdio.h>

int main() {

    int A;
    scanf("%d", &A);
    int B;
    scanf("%d", &B);
    int C;
    scanf("%d", &C);
    int D;
    scanf("%d", &D);
    int DIFERENCA;

    DIFERENCA = (A * B - C * D);

    printf("DIFERENCA = %d\n", DIFERENCA);

    return 0;
}